import { StyleSheet } from "react-native"; 
const styles = StyleSheet.create({
    button:{
        marginTop: 14,
        alignSelf: 'center',
    },
    text:{
        color: '#a1a1a1',
    }
});

export default componentStyle;
